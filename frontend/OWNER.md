# Frontend
**Owner: Kirubalan — Frontend**

## Scope
Everything under frontend/ — pages, components, hooks, state, styling.

## Contract with backend
- Call only the documented API endpoints (see docs/TransitOps_Winning_Blueprint.md Section 8)
- Do not assume DB field names — use the types agreed in backend/src/shared/types/
- If an endpoint you need doesn't exist yet, add a note in #blockers on the team
  channel rather than guessing the shape — avoids rework when the real endpoint lands
